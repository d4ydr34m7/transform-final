Place the Nasdaq logo (blue "N" ribbon image) here as navbar-logo.png so the navbar displays it.
Vite serves files from this folder at the root, so the image will be at /navbar-logo.png.

If the file is missing, the navbar falls back to the CSS geometric icon.
